[project]
name = "infra-fail-mngr"
version = "0.1.0"
description = "Infrastructure failure manager agent"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[tool.setuptools]
package-dir = {"" = "src"}

[tool.uv]
package = true

